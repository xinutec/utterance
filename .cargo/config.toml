# Pin ts-rs's export directory so every `cargo test` — local, CI, the drift gate
# — writes the generated types to the frontend's generated/ dir instead of
# ts-rs's default ./bindings. `relative = true` resolves it against this root.
[env]
TS_RS_EXPORT_DIR = { value = "frontend/src/app/generated", relative = true }
