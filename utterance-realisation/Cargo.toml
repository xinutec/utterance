[package]
name = "utterance-realisation"
version = "0.1.0"
edition = "2024"

[dependencies]
# Reads a score and nothing else. It must not know what a scale is — see
# utterance-mapping/src/score.rs for why that is the whole point of the interface.
utterance-mapping = { path = "../utterance-mapping" }
# WAV container only, the same one analysis decodes with.
hound = "3.5.1"

[lints]
workspace = true
