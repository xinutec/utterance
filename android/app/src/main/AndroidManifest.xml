<?xml version="1.0" encoding="utf-8"?>
<!-- dev-lint: android-webview (held identical to dev-lint's canonical WebView manifest) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The page calls getUserMedia: voice is the input this app exists for. The
         WebView's own PermissionRequest is granted in MainActivity, but only once
         Android has granted this one — the two are separate gates and the page
         hangs silently if either is missing. Per-app permissions are a sanctioned
         slot in dev-lint's canonical WebView manifest. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Utterance">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
